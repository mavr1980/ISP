# ISP

Приложение для расчёта теплопоступлений от солнечной радиации через ограждающие конструкции. Реализовано на VB.NET в виде Windows Forms.

## Сборка и запуск

Для сборки требуется .NET 8 SDK для Windows:

```bash
dotnet build
```

Получившийся исполняемый файл можно запустить на Windows.
